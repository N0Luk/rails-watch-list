<h1>All Movies</h1>
<% @movies.each do |movie| %>
  <div class="movie">
    <%= image_tag(movie.poster_url, class: "poster") %>
    <h2><%= movie.title %></h2>
    <p><%= movie.overview %></p>
    <p>Rating: <%= movie.rating %></p>
  </div>
<% end %>
